$("#dyr1").datepicker( {
    format: " yyyy",
    viewMode: "years", 
    minViewMode: "years"
});

$("#dmy1").datepicker( {
    format: "mm-yyyy",
    viewMode: "months", 
    minViewMode: "months"
});

$("#dmy2").datepicker( {
    format: "mm-yyyy",
    viewMode: "months", 
    minViewMode: "months"
});